const BigTitle = ({ children }) => {
  return (
    <h2 className="text-[1.75rem] uppercase font-semibold mb-[3rem] hidden lg:block lg:pb-[1rem]">
      {children}
    </h2>
  );
};

export default BigTitle;
